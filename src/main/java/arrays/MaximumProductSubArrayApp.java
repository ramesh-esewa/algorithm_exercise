package arrays;

/**
 * @author Ramesh Khanal
 * 9/30/2023
 */
public class MaximumProductSubArrayApp {
    public static void main(String[] args) {
        int[] arr = { 1, 2, -5, -4 };
        int product = -10;
        findMaxProduct(arr, product);
    }

    private static void findMaxProduct(int[] arr, int product) {
        int minVal = arr[0];
        int maxVal = arr[0];
        int maxProduct = arr[0];

        for (int i = 1; i < arr.length; i++) {

        }
    }
}
